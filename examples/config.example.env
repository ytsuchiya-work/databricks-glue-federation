#
# Databricks Unity Catalog → AWS Glue Federation Configuration
#
# Copy this file to .env and fill in your values:
#   cp examples/config.example.env .env
#
# Then run:
#   ./setup.sh --config .env
#

# ============================================================================
# REQUIRED SETTINGS
# ============================================================================

# A prefix for naming AWS resources (e.g., "myproject" → "myproject-glue-role")
PREFIX="my-federation"

# Your Databricks workspace URL
# Find this in your browser when logged into Databricks
# Example: https://dbc-a1b2c3d4-e5f6.cloud.databricks.com
DATABRICKS_WORKSPACE_URL="https://your-workspace.cloud.databricks.com"

# The Unity Catalog you want to federate
# Find this in Databricks → Catalog Explorer
# Example: main, production, my_catalog
UC_CATALOG_NAME="my_catalog"

# Databricks OAuth Client ID (Service Principal Application ID)
# Find this in: Account Console → User Management → Service Principals
DATABRICKS_CLIENT_ID="your-client-id-here"

# Databricks OAuth Client Secret
# Created when setting up the Service Principal
DATABRICKS_CLIENT_SECRET="your-client-secret-here"

# S3 bucket where your table data is stored
# Find this in Unity Catalog storage location settings
S3_BUCKET_NAME="your-data-bucket"

# AWS Region
# Should match where your data and Databricks workspace are
AWS_REGION="us-west-2"

